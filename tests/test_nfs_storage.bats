#!/usr/bin/env bats
# nfs-client storage class can provision a PVC and a pod can mount + write to it.
load helpers

@test "nfs-client PVC provisions and pod can write to it" {
  skip "need to change this test up"
}
